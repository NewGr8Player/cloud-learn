spring:
  application:
    name: config-client-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        password: nacos
        username: nacos
        namespace: dev
        group: DEV-FZM